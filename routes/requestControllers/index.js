const todoController = require('./todoController');
const userController = require('./userController');

const requestControllers = { todoController, userController };

module.exports = requestControllers;
